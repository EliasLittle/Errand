Read about it [here](https://www.nushell.sh/).

Fantastic error messages
> Nu operates on typed data, so it catches bugs that other shells don't. And when things break, Nu tells you exactly where and why:
![[Pasted image 20230420180844.png]]

Default data structure is a table. Functions return (or are cast to) tables.
This means that, unlike standard Unix, functions interoperate easily and you don't have to constantly string-encode/decode and parse custom text formats.

